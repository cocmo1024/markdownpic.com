---
title: How to write an email link in Markdown
description: How to write an email link in Markdown with mailto syntax, what breaks across renderers and exports, and how to keep the result portable.
referenceType: post
adProfile: post
commercialIntent: medium
reviewCadence: Quarterly review
contentStatus: cornerstone
problemSolved: Shows the safest way to create email links in Markdown and explains where mailto links fail or stop mattering.
readerTakeaway: You should leave with the correct Markdown syntax for email links, a reliable decision rule, and a clear understanding of image-export limitations.
primaryKeyword: how to write an email link in markdown
searchIntent: learn
decisionStage: implementation
targetRoles:
  - Writers
  - Developers
  - Documentation teams
keyQuestions:
  - What is the correct Markdown syntax for an email link?
  - Are angle-bracket email links reliable?
  - What happens to email links in screenshots and image exports?
notFor:
  - Readers looking for advanced email-automation or tracking-link setups
updateTriggers:
  - Meaningful renderer behavior changes around autolinks
  - Major MarkdownPic export behavior changes that affect interactive links
contentCluster: markdown-publishing-workflows
canonicalCluster: markdown-publishing-workflows
pageType: publishing-guide
lifecycle: evergreen
refreshPriority: medium
authorKey: editorial-desk
editorKey: review-desk
lastReviewed: 2026-05-09
---

## What matters first

If you want the most portable and explicit form, write the email link as a normal Markdown link whose URL uses the `mailto:` scheme:

```md
[Email us](mailto:hello@example.com)
```

That works more reliably than hoping a renderer auto-links a naked email address.

## Why `mailto:` is the safest form

Markdown does not have a special email-link primitive. It has links. An email address becomes a link only when the renderer or the author turns it into one.

Using `mailto:` gives you:

- explicit intent,
- consistent link text,
- predictable behavior across HTML outputs,
- and cleaner reuse in docs sites, blogs, and READMEs.

## The other form people use

Some environments also auto-link angle-bracket email addresses:

```md
<hello@example.com>
```

That can work, but it is less portable because the behavior depends more on the renderer.

## When plain text is actually better

Sometimes you should not create a clickable email link at all.

Examples:

- a long image export,
- a visual card,
- a public screenshot where copyability matters more than clickability,
- or a place where spam harvesting risk is more important than convenience.

In those cases, visible plain text may be the better answer.

## Image export boundary

A `mailto:` link only matters while the output is still HTML or another interactive format. Once the content becomes a PNG or long image, the link is visual only.

That is why Markdown-to-image workflows often need a different decision:

- clickable link for HTML,
- visible address or contact line for image assets.

## Recommended rule

Use `mailto:` when the Markdown is staying interactive. Use visible text when the content is being flattened into a static visual asset.

If the file needs to move across several renderers, do not rely on automatic email linking.

## Useful `mailto:` variants

Markdown links can include common `mailto:` options:

```md
[Email support](mailto:support@example.com)
[Email support with subject](mailto:support@example.com?subject=Support%20request)
```

Use these carefully. Query parameters must be URL-encoded, and long prefilled messages become hard to read and maintain in Markdown source.

For most public docs, the safest version is still the simple one:

```md
[Contact support](mailto:support@example.com)
```

It is readable in source, clear in rendered HTML, and less likely to break during copy-paste.

## When not to prefill email fields

Avoid complex prefilled email links when:

- the Markdown will be reused in a CMS or email platform;
- the link may be flattened into an image or PDF;
- localization or translation will edit the text;
- the address may change often;
- privacy or spam exposure matters.

In those cases, use a plain contact line or a contact page link instead. A contact page is often easier to update than many scattered `mailto:` links.

## Troubleshooting checklist

If the email link does not work:

1. confirm the URL starts with `mailto:`;
2. check that spaces in subject text are encoded as `%20` or `+`;
3. test the rendered HTML, not only the Markdown preview;
4. remember that PNG, screenshot, and long-image exports cannot preserve clickable links;
5. check whether the publishing platform rewrites or sanitizes link protocols.

This checklist catches the common failures without turning a simple email link into an overengineered tracking system.

## Related pages

- [Markdown links: inline, reference, and autolinks](/syntax/markdown-links-inline-vs-reference-and-autolinks/)
- [Markdown to image workflows for docs, social, and AI content](/guides/markdown-to-image-workflows-for-docs-social-and-ai-content/)
- [Markdown image syntax: local vs. remote paths](/syntax/markdown-image-syntax-local-vs-remote-paths/)
