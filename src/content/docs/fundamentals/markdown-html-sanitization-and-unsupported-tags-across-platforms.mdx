---
title: Markdown HTML sanitization and unsupported tags across platforms
description: How Markdown renderers sanitize or reject raw HTML, which tags commonly break, and how to write source that survives GitHub, docs sites, CMSs, and export tools.
referenceType: fundamental
adProfile: fundamental
commercialIntent: medium
reviewCadence: Monthly review
contentStatus: growing
problemSolved: Helps authors understand why raw HTML survives in some Markdown pipelines and disappears, degrades, or gets stripped in others.
readerTakeaway: You should leave with a safer model for when raw HTML is acceptable, which tags are likely to fail, and how to avoid portability debt.
primaryKeyword: markdown html sanitization
searchIntent: learn
decisionStage: problem-aware
targetRoles:
  - Developers
  - Technical writers
  - Docs teams
  - Content operators
keyQuestions:
  - Why does raw HTML render in one Markdown platform and break in another?
  - Which HTML tags are most likely to be sanitized or unsupported?
  - How should teams write Markdown when sanitization rules are outside their control?
notFor:
  - HTML-only publishing stacks
  - Readers looking only for a Markdown syntax cheat sheet
updateTriggers:
  - Material changes in common Markdown renderer sanitization behavior
  - Platform changes affecting supported HTML tags or unsafe content rules
contentCluster: markdown-portability
canonicalCluster: markdown-portability
pageType: reference
lifecycle: evergreen
refreshPriority: medium
authorKey: editorial-desk
editorKey: review-desk
lastReviewed: 2026-05-09
---

# Markdown HTML sanitization and unsupported tags across platforms

Raw HTML is one of the most misunderstood parts of Markdown authoring. Authors often see a tag render once and assume it is safe everywhere. That assumption breaks quickly when the same source file moves between GitHub, docs platforms, CMS renderers, export tools, note apps, or image-generation workflows.

## What matters first

Raw HTML in Markdown is not governed by one universal rendering contract.

Three different things can happen:

- the HTML is allowed and rendered;
- the HTML is parsed but sanitized or partially stripped;
- the HTML is rejected, escaped, or ignored by the renderer.

If the source must travel, treat raw HTML as a **renderer-specific dependency**, not portable Markdown.

## Which tags usually fail first

The highest-risk HTML patterns are usually:

- `<script>`, `<style>`, and similar active or potentially unsafe tags;
- iframes, embeds, and arbitrary media wrappers;
- custom div or span patterns that rely on site CSS;
- HTML that assumes custom IDs, classes, or JavaScript behavior;
- anything that expects the Markdown renderer to behave like a full HTML document.

These patterns often look fine in one publishing stack and collapse elsewhere.

## Platform behavior examples

The exact rules differ, but the pattern is consistent:

| HTML pattern | What often happens | Safer Markdown-first alternative |
| --- | --- | --- |
| `<script>` or inline event handlers | Removed, escaped, or blocked for security | Do not put scripts in Markdown; use the site layer |
| `<iframe>` embeds | Blocked or allowed only by specific platforms | Link to the asset or use an approved embed component |
| `<style>` or inline layout CSS | Stripped, ignored, or inconsistent | Move styling into the theme or renderer |
| `<div class="...">` layout wrappers | Renders only where the CSS exists | Use standard Markdown or documented components |
| `<br>` for line breaks | Often works, but may be undesirable in portable source | Prefer lists, paragraphs, or tested hard-break syntax |
| Custom attributes | Removed by sanitizers | Avoid unless the renderer documents support |

This table is not a universal compatibility chart. It is a risk map. The more a tag depends on browser behavior, custom CSS, or JavaScript, the less portable it is as Markdown source.

## Sanitization is not random

Sanitization usually follows one of these motives:

| Motive | What it means for the author |
| --- | --- |
| Security | Scripts, embeds, and unsafe tags are stripped or blocked |
| Content integrity | Platforms reject layout tricks that break consistency |
| Portability | Minimal renderers simply do not support richer HTML behavior |
| Product simplicity | Some editors and viewers intentionally ignore complex raw HTML |

That means "it got removed" is often the platform working exactly as designed.

## A safer authoring policy

If Markdown is the canonical source, a safer policy is:

1. prefer standard Markdown first;
2. allow raw HTML only for documented exceptions;
3. keep those exceptions small and visible;
4. test them across the actual render path, not just one editor preview.

This keeps HTML as an exception layer instead of turning the source into fragile pseudo-HTML.

## Decision tree for raw HTML

Before adding raw HTML, ask:

1. Can standard Markdown express the content clearly?
2. Is the desired behavior content, layout, or interactivity?
3. Does the target renderer explicitly support the tag?
4. Will the same file be reused in GitHub, docs sites, CMSs, PDF, email, or image export?
5. Will a future editor understand why the HTML is there?

If the behavior is layout or interactivity, the answer usually belongs outside the Markdown file. If the behavior is content and the renderer documents support, the HTML may be acceptable with a comment or editorial convention.

## Example: acceptable vs risky HTML

Acceptable in a controlled docs site:

```md
This line uses an approved inline HTML break only because the renderer and export path are known.<br>
Second line.
```

Risky as portable Markdown:

```md
<div class="two-column special-callout" onclick="trackClick()">
  <iframe src="https://example.com/embed"></iframe>
</div>
```

The second example depends on CSS, JavaScript, iframe policy, sanitizer rules, and export behavior. It is no longer just Markdown content.

## How teams should document exceptions

If a team allows raw HTML, keep an exception list:

| Exception | Allowed where | Why |
| --- | --- | --- |
| `<br>` | Known docs renderer only | Addresses or poems where line breaks are content |
| `<kbd>` | Developer docs where supported | Keyboard shortcuts are semantic enough to justify the tag |
| Approved embed component | Site docs only | The site owns the security and rendering behavior |

This prevents every author from rediscovering the same renderer limits through broken pages.

## What portable teams should avoid

Avoid treating raw HTML as the default solution for alignment, layout, responsive embeds, custom classes, font sizing, or design embellishment. Those usually belong in the renderer, the site theme, or the export layer, not the Markdown source.

## The practical decision rule

Use raw HTML in Markdown only when:

- the render path is known;
- the behavior is documented or repeatable;
- the source is not expected to move broadly;
- and the HTML solves a real publishing need, not cosmetic preference alone.

If those conditions are missing, the safer choice is usually simpler Markdown plus a better rendering layer.

## Related pages

- [Markdown links vs HTML links: when to switch](/syntax/markdown-links-vs-html-links-when-to-switch/)
- [Markdown iframe, video, and embed limitations](/syntax/markdown-iframe-video-and-embed-limitations/)
- [Copy-paste-safe Markdown for chat, docs, and CMS workflows](/guides/copy-paste-safe-markdown-for-chat-docs-and-cms/)
