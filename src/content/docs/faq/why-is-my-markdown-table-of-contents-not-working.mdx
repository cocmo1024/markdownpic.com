---
title: Why is my Markdown table of contents not working?
description: Fix Markdown tables of contents by checking anchor support, stale manual links, renderer plugins, and heading IDs that changed after publishing.
referenceType: faq
adProfile: faq
commercialIntent: high
reviewCadence: Quarterly review
contentStatus: growing
problemSolved: Helps authors debug broken Markdown tables of contents by separating manual link mistakes from anchor instability, plugin assumptions, and renderer differences.
readerTakeaway: You should leave with a practical way to tell whether the TOC problem comes from stale anchor links, missing generator support, or a publishing system that rewrote heading IDs.
primaryKeyword: why is my markdown table of contents not working
searchIntent: learn
decisionStage: problem-aware
targetRoles:
  - Markdown learners
  - Developers
  - Technical writers
  - Documentation teams
keyQuestions:
  - Why do Markdown TOCs break after edits or publishing?
  - Does Markdown have a universal TOC syntax?
  - When is the problem really anchor generation instead of the TOC itself?
contentCluster: markdown-troubleshooting
canonicalCluster: markdown-troubleshooting
pageType: question-answer
lifecycle: evergreen
refreshPriority: high
authorKey: editorial-desk
editorKey: review-desk
lastReviewed: 2026-05-09
---

import { LinkCard } from '@astrojs/starlight/components';

# Why is my Markdown table of contents not working?

## What matters first

A Markdown table of contents usually breaks because:

1. the links point to anchors that changed,
2. the renderer does not support the TOC format you expected,
3. a plugin-generated TOC was never rebuilt,
4. heading IDs changed after editing or publishing,
5. or the platform does not define TOC behavior the way your editor did.

The most common mistake is assuming Markdown has one universal TOC syntax. It does not.

## Markdown does not define one TOC standard

Markdown supports headings and links.

It does **not** define one portable table-of-contents mechanism that every renderer must generate the same way.

That means TOCs are usually built with:

- manual anchor links,
- renderer-generated navigation,
- or a plugin or tool that inserts the TOC for you.

Each path breaks differently.

## Manual TOCs go stale easily

A manual TOC fails when:

- heading text changes,
- duplicate headings create different slugs,
- punctuation changes the fragment,
- or the published renderer generates different IDs than the local tool.

That is why the TOC may look correct in source while the links still fail.

## Generated TOCs depend on the renderer

Generated TOCs fail when:

- the site does not support the generator,
- the plugin uses different slug rules,
- the insertion step did not run,
- or the final publishing pipeline ignored the local tool's TOC behavior.

At that point the problem is not only Markdown. It is renderer behavior.

## The safest troubleshooting order

Check in this order:

1. confirm whether the TOC is manual or generated,
2. inspect whether the target headings still exist,
3. compare the rendered heading IDs with the links in the TOC,
4. check whether a plugin or generator actually ran,
5. then inspect whether the publish layer changed the anchors.

That sequence catches most TOC failures quickly.

## Manual TOC example

A manual TOC is just a list of links to heading fragments:

```md
## Contents

- [Install](#install)
- [Configuration](#configuration)
- [Troubleshooting](#troubleshooting)

## Install

...

## Configuration

...

## Troubleshooting

...
```

This works only if the rendered heading IDs match those fragments. If the platform generates `#configuration-1` because another heading already used the same text, the manual TOC will break.

## Duplicate headings are a common cause

Duplicate headings create unstable anchors:

```md
## Setup

...

## Setup

...
```

Many renderers will turn the second heading into something like `#setup-1`, but the exact rule can differ. A safer authoring habit is to make headings unique:

```md
## Local setup

...

## Production setup

...
```

Unique headings make both manual TOCs and generated navigation more reliable.

## Generated TOC checks

If a tool generates the TOC, verify:

- whether the generator runs before publishing;
- whether it reads the same file that gets published;
- whether it includes only `h2` headings or deeper levels;
- whether the final site rewrites heading IDs after generation;
- whether the generated block is overwritten by a formatter or CMS import.

The broken link may not be in Markdown at all. It may be in the build order.

## Portable TOC policy

For documents that move between GitHub, docs sites, CMSs, PDF exporters, and AI workflows:

- keep headings unique and stable;
- avoid punctuation-heavy headings if you need manual anchors;
- prefer generated navigation when the publishing system owns the final render;
- use manual TOCs only for stable files that humans maintain deliberately;
- test the published page, not only the editor preview.

This policy is more important than memorizing one slug algorithm, because the slug algorithm can change across platforms.

## The healthiest long-term habit

Use manual TOCs only when the document is stable enough to maintain them.

Use generated TOCs when the publishing system owns heading navigation reliably.

The real risk is not TOC syntax. It is anchor stability over time.

## Compare next

<div class="portal-links">
	<LinkCard
		title="Markdown table of contents options and anchor stability"
		href="/syntax/markdown-table-of-contents-options-and-anchor-stability/"
		description="Use this page for the deeper model behind manual TOCs, generated TOCs, and anchor-safe heading structure."
	/>
	<LinkCard
		title="Why is my Markdown anchor link not working?"
		href="/faq/why-is-my-markdown-anchor-link-not-working/"
		description="Use this page when the TOC problem is really a fragment and heading-ID problem."
	/>
	<LinkCard
		title="Markdown anchors and heading links that stay stable"
		href="/syntax/markdown-anchors-and-heading-links-that-stay-stable/"
		description="Use this page when heading slugs and fragment rules are the unstable part of the document."
	/>
	<LinkCard
		title="Can Markdown link to a heading in another file?"
		href="/faq/can-markdown-link-to-a-heading-in-another-file/"
		description="Use this page when the TOC or navigation needs cross-file heading links rather than same-page jumps."
	/>
</div>
